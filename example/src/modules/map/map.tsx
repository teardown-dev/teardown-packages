import {FunctionComponent} from "react";
import {MapPortal} from "./components/map-portal.tsx";

export type MapProps = {};

export const Map: FunctionComponent<MapProps> = (props) => {
    const {} = props;
    return (
        <div className="">
            {/*<MapPortal />*/}
        </div>
    );
};
