




To get started:
1. Install nativewind v4 https://www.nativewind.dev/v4/getting-started/react-native

2.Install other external dependencies:
```bash
yarn add react-native-gesture-handler react-native-reanimated react-native-svg
```
3. Install pods
```bash
cd ./ios && pod install

or

npx pod-install
```
