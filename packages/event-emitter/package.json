{
  "name": "@teardown/event-emitter",
  "version": "1.1.0-beta-05",
  "description": "",
  "scripts": {
    "prepublishOnly": "bun run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "bun run update-deps && tsc --declaration",
    "dev": "tsc --declaration --watch",
    "update-deps": "bun scripts/update-dependencies.ts"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "README.md",
    "./dist/**/*"
  ],
  "dependencies": {
    "@teardown/config": "^1.1.0-beta-05",
    "@teardown/util": "^1.1.0-beta-05",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.1"
  }
}
